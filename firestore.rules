rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // With login removed, all collections are publicly readable and writable.
    // This is not secure for a production application with sensitive data.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
