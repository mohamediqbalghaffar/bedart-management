{
  "entities": {
    "SellingForm": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SellingForm",
      "type": "object",
      "description": "Represents a sales form for the mattress and bedding shop.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the SellingForm entity." },
        "creatorId": { "type": "string", "description": "The UID of the user who created the form." },
        "creatorName": { "type": "string", "description": "The name of the user who created the form." },
        "formNumber": { "type": "string", "description": "Auto-incrementing form number." },
        "customerName": { "type": "string", "description": "Name of the customer." },
        "customerPhoneNumber": { "type": "string", "description": "Customer's phone number." },
        "customerAddress": { "type": "string", "description": "Customer's address." },
        "issueDate": { "type": "string", "description": "Date the form was issued.", "format": "date-time" },
        "deliveryCost": { "type": "number", "description": "Optional delivery cost." },
        "discountType": { "type": "string", "enum": ["percentage", "cash"], "description": "The type of discount applied."},
        "discountValue": { "type": "number", "description": "The value of the discount applied."},
        "isDeliveryPaid": { "type": "boolean", "description": "Indicates if the delivery cost has been paid." },
        "totalPrice": { "type": "number", "description": "Total price of the products and delivery." },
        "paymentStatus": { "type": "string", "description": "Payment status: Unpaid, Partially Paid, Fully Paid." },
        "paymentType": { "type": "string", "description": "Payment type: After delivery, Installments, Pre-order." },
        "remainingBalance": { "type": "number", "description": "Remaining balance to be paid." },
        "products": { "type": "array", "description": "Array of product details.", "items": { "type": "string" } },
        "payments": { "type": "array", "description": "Array of payment details.", "items": { "type": "string" } }
      },
      "required": ["id", "creatorId", "formNumber", "customerName", "customerPhoneNumber", "customerAddress", "issueDate", "totalPrice", "paymentStatus", "paymentType"]
    },
    "BuyingForm": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BuyingForm",
      "type": "object",
      "description": "Represents a buying form for purchasing products from suppliers.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the BuyingForm entity." },
        "supplierId": { "type": "string", "description": "Reference to Supplier. (Relationship: Supplier 1:N BuyingForm)" },
        "issueDate": { "type": "string", "description": "Date the form was issued.", "format": "date-time" },
        "customsFee": { "type": "number", "description": "Customs or country entry fee." },
        "products": { "type": "array", "description": "Array of product details.", "items": { "type": "string" } },
        "totalAmount": { "type": "number", "description": "The total amount of the buying form, calculated on save." }
      },
      "required": ["id", "supplierId", "issueDate", "totalAmount"]
    },
    "Expense": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Expense",
      "type": "object",
      "description": "Represents an expense record.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Expense entity." },
        "name": { "type": "string", "description": "Name or title of the expense." },
        "date": { "type": "string", "description": "Date of the expense.", "format": "date-time" },
        "amount": { "type": "number", "description": "Amount of the expense." },
        "currency": { "type": "string", "description": "The currency of the expense.", "enum": ["USD", "IQD"] },
        "category": { "type": "string", "description": "Category of the expense: Daily, Salary, Rent, Electricity, Transport, Other." },
        "note": { "type": "string", "description": "Optional note about the expense." },
        "sellingFormId": { "type": "string", "description": "Reference to SellingForm if the expense is linked to a sale. (Relationship: SellingForm 1:N Expense)" }
      },
      "required": ["id", "name", "date", "amount", "category", "currency"]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product in the warehouse or shop.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Product entity." },
        "productName": { "type": "string", "description": "Name of the product." },
        "category": { "type": "string", "description": "Category of the product: Mattress, Bed, Pillow, Cover." },
        "sizeModel": { "type": "string", "description": "Size or model of the product." },
        "supplierId": { "type": "string", "description": "Reference to Supplier if known. (Relationship: Supplier 1:N Product)" },
        "stockLocation": { "type": "string", "description": "Stock location: Warehouse, Shop Showroom." },
        "currentQuantity": { "type": "number", "description": "Current quantity in stock (auto-calculated)." },
        "sellingPrice": { "type": "number", "description": "The price at which the product is sold." }
      },
      "required": ["id", "productName", "category", "stockLocation", "currentQuantity"]
    },
    "StockMovement": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StockMovement",
      "type": "object",
      "description": "Represents a stock movement record (in or out).",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the StockMovement entity." },
        "productId": { "type": "string", "description": "Reference to Product. (Relationship: Product 1:N StockMovement)" },
        "quantity": { "type": "number", "description": "Quantity of the product moved." },
        "fromLocation": { "type": "string", "description": "From location: Warehouse, Shop." },
        "toLocation": { "type": "string", "description": "To location: Warehouse, Shop." },
        "date": { "type": "string", "description": "Date of the stock movement.", "format": "date-time" },
        "note": { "type": "string", "description": "Optional note about the stock movement." },
        "buyingFormId": { "type": "string", "description": "Reference to BuyingForm if the movement is due to buying. (Relationship: BuyingForm 1:N StockMovement)" },
        "sellingFormId": { "type": "string", "description": "Reference to SellingForm if the movement is due to selling. (Relationship: SellingForm 1:N StockMovement)" }
      },
      "required": ["id", "productId", "quantity", "fromLocation", "toLocation", "date"]
    },
    "Supplier": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Supplier",
      "type": "object",
      "description": "Represents a supplier of products.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Supplier entity." },
        "supplierName": { "type": "string", "description": "Name of the supplier." },
        "contactInformation": { "type": "string", "description": "Supplier contact information." }
      },
      "required": ["id", "supplierName"]
    },
    "Payment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Payment",
      "type": "object",
      "description": "Represents a payment record.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Payment entity." },
        "sellingFormId": { "type": "string", "description": "Reference to SellingForm. (Relationship: SellingForm 1:N Payment)" },
        "paymentDate": { "type": "string", "description": "Date of the payment.", "format": "date-time" },
        "amountPaid": { "type": "number", "description": "Amount paid." },
        "paymentMethod": { "type": "string", "description": "Payment method: Cash, Transfer." },
        "note": { "type": "string", "description": "Optional note about the payment." }
      },
      "required": ["id", "sellingFormId", "paymentDate", "amountPaid", "paymentMethod"]
    },
    "Customer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Customer",
      "type": "object",
      "description": "Represents a customer in the CRM.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Customer entity." },
        "customerName": { "type": "string", "description": "The name of the customer." },
        "customerPhoneNumber": { "type": "string", "description": "The customer's phone number." },
        "customerAddress": { "type": "string", "description": "The customer's address." }
      },
      "required": ["id", "customerName"]
    },
    "ProductSellingForm": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ProductSellingForm",
      "type": "object",
      "description": "Represents the association between products and selling forms, capturing quantity and price.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the ProductSellingForm entity." },
        "sellingFormId": { "type": "string", "description": "Reference to SellingForm. (Relationship: SellingForm N:N Product)" },
        "productId": { "type": "string", "description": "Reference to Product. (Relationship: Product N:N SellingForm)" },
        "quantity": { "type": "number", "description": "Quantity of the product sold." },
        "unitPrice": { "type": "number", "description": "Price of a single unit of the product." },
        "lineTotal": { "type": "number", "description": "Total price for the line item (Quantity * Unit Price)." }
      },
      "required": ["id", "sellingFormId", "productId", "quantity", "unitPrice", "lineTotal"]
    },
    "ProductBuyingForm": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ProductBuyingForm",
      "type": "object",
      "description": "Represents the association between products and buying forms, capturing quantity and price.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the ProductBuyingForm entity." },
        "buyingFormId": { "type": "string", "description": "Reference to BuyingForm. (Relationship: BuyingForm N:N Product)" },
        "productId": { "type": "string", "description": "Reference to Product. (Relationship: Product N:N BuyingForm)" },
        "quantity": { "type": "number", "description": "Quantity of the product bought." },
        "unitPrice": { "type": "number", "description": "Price of a single unit of the product." },
        "landedCost": { "type": "number", "description": "The total landed cost per product after customs." }
      },
      "required": ["id", "buyingFormId", "productId", "quantity", "unitPrice"]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents an application user.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the User entity." },
        "name": { "type": "string", "description": "The user's name." },
        "role": { "type": "string", "description": "The user's role.", "enum": ["Admin", "Data Manager", "Salesman"] }
      },
      "required": ["id", "name", "role"]
    }
  },
  "firestore": {
    "/customers/{customerId}": {
      "schema": { "$ref": "#/entities/Customer" },
      "description": "Stores customer information."
    },
    "/selling_forms/{sellingFormId}": {
      "schema": { "$ref": "#/entities/SellingForm" },
      "description": "Stores selling forms data. Includes denormalized data for total price, payment status and payment type."
    },
    "/selling_forms/{sellingFormId}/payments/{paymentId}": {
      "schema": { "$ref": "#/entities/Payment" },
      "description": "Stores payment data related to a specific selling form."
    },
    "/selling_forms/{sellingFormId}/products/{productSellingFormId}": {
      "schema": { "$ref": "#/entities/ProductSellingForm" },
      "description": "Stores product data related to a specific selling form. Includes quantity and prices."
    },
    "/buying_forms/{buyingFormId}": {
      "schema": { "$ref": "#/entities/BuyingForm" },
      "description": "Stores buying forms data."
    },
    "/buying_forms/{buyingFormId}/products/{productBuyingFormId}": {
      "schema": { "$ref": "#/entities/ProductBuyingForm" },
      "description": "Stores product data related to a specific buying form. Includes quantity and prices."
    },
    "/expenses/{expenseId}": {
      "schema": { "$ref": "#/entities/Expense" },
      "description": "Stores expense records."
    },
    "/products/{productId}": {
      "schema": { "$ref": "#/entities/Product" },
      "description": "Stores product information."
    },
    "/stock_movements/{stockMovementId}": {
      "schema": { "$ref": "#/entities/StockMovement" },
      "description": "Stores stock movement records."
    },
    "/suppliers/{supplierId}": {
      "schema": { "$ref": "#/entities/Supplier" },
      "description": "Stores supplier information."
    },
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user account information."
    }
  }
}
